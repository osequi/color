// Change color wheel colors by applying a color function
// - tunerType: tint, shade, tone, ...
// - tunerValue: 0-100%
// - colorValue: #xxyyxx
var changeColorWheelColors = function(tunerType, tunerValue, colorValue) {
  
}
